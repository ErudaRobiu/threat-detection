# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (secrets — never commit)
.env
.env.local
.env*.local

# local database (Phase 4)
/data/*.db
/data/*.db-*

# gemini response cache (SHA-256 keyed; regenerated on demand)
/.cache/

# evaluation raw corpora (reproduce with eval/fetch_corpora.sh) and cache
/eval/data/
/eval/cache.json
/eval/cache.json.tmp
# generated figures + metric CSVs (reproduce: run.py/baseline.py/gamma_sweep.py/charts.py)
/eval/out/

# real redacted emails dropped for the hard-case set (never committed; keep README)
/eval/hardcases_real/*
!/eval/hardcases_real/README.md
/eval/hardcases_cache.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env*
